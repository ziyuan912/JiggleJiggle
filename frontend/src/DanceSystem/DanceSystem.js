
class DanceSystem {
  constructor() {
    this.userPoses = []; 
    this.targetPoses = [];
  }

  evaluatePoses() {
    let score = Math.random();

    /* Code to evaluate poses here. */

    return score;
  }
}

export const danceSystem = new DanceSystem();